<title>AWS Bedrock AgentCore OpenAPI 梳理（参考格式版，2026-06-04）</title>

<h1>AWS Bedrock AgentCore 平台核心组件 OpenAPI 梳理（2026-06-04）</h1>

<callout background-color="light-blue" border-color="blue">
  <p>本版按参考文档的 API 手册格式重排：先定义调用前置条件与资源边界，再给出模块总览，随后按 Runtime、Harness、Memory、Gateway、Identity、Browser、Code Interpreter、Evaluations、Payments、Policy / Registry 拆成可落 OpenAPI 的资源表。特别注意：Gateway 的工具调用面是 MCP endpoint，不应伪造成普通 REST path；OpenAPI 只适合描述 Gateway target schema 或控制面 CRUD。</p>
</callout>

<h2>调用前置条件与资源边界</h2>

<table>
  <thead>
    <tr><th background-color="light-gray">项目</th><th background-color="light-gray">要求或含义</th><th background-color="light-gray">实践提示</th></tr>
  </thead>
  <tbody>
    <tr><td>Service prefix</td><td><code>bedrock-agentcore</code></td><td>IAM policy、CloudTrail、Service Authorization Reference 中使用该前缀。</td></tr>
    <tr><td>Control plane endpoint</td><td><code>bedrock-agentcore-control.{region}.amazonaws.com</code></td><td>用于创建、更新、删除 Runtime、Memory、Gateway、Identity、Browser、Code Interpreter、Evaluator、Policy、Registry、Payments 等资源。</td></tr>
    <tr><td>Data plane endpoint</td><td><code>bedrock-agentcore.{region}.amazonaws.com</code></td><td>用于 runtime/harness invoke、memory event 与 records、browser/code session、evaluation job、payment session 等运行时调用。</td></tr>
    <tr><td>Gateway data plane endpoint</td><td><code>{gatewayId}.gateway.bedrock-agentcore.{region}.amazonaws.com</code></td><td>用于 Gateway MCP 工具发现和调用；替换 <code>gatewayId</code> 后按 MCP client 方式访问。</td></tr>
    <tr><td>认证</td><td>AWS API 走 IAM/SigV4；Runtime/Gateway inbound authorizer 可配置 <code>AWS_IAM</code>、<code>CUSTOM_JWT</code>、<code>NONE</code>、<code>AUTHENTICATE_ONLY</code> 等模式。</td><td>OpenAPI 文档中应把 SigV4、JWT、自定义 authorizer 分开建 security scheme，避免把所有 API 简化为 bearer token。</td></tr>
    <tr><td>执行角色</td><td>创建资源常需要 runtime role、gateway role、memory execution role、KMS key 等。</td><td>调用方通常需要 <code>iam:PassRole</code>；生产落地时把 role、KMS、VPC/S3 endpoint、resource policy 作为部署前置检查。</td></tr>
    <tr><td>Gateway target schema</td><td>支持 OpenAPI 3.0/3.1 和 Smithy target，但不是所有 schema 特性都适合直接转换成工具。</td><td>OpenAPI target 必须有稳定 <code>operationId</code>；复杂 <code>oneOf/anyOf/allOf</code>、callbacks、webhooks、复杂参数序列化建议先降级或手工适配。</td></tr>
  </tbody>
</table>

<whiteboard type="mermaid">flowchart LR
  Client["SDK / CLI / Console"] --&gt; CP["Control Plane&lt;br/&gt;bedrock-agentcore-control"]
  Client --&gt; DP["Data Plane&lt;br/&gt;bedrock-agentcore"]
  Agent["MCP Client / Agent Runtime"] --&gt; GP["Gateway Data Plane&lt;br/&gt;gateway URL"]
  CP --&gt; Runtime["Runtime / Harness"]
  CP --&gt; Memory["Memory"]
  CP --&gt; Gateway["Gateway / Targets / Rules"]
  CP --&gt; Identity["Identity / Token Vault"]
  CP --&gt; Tools["Browser / Code Interpreter"]
  CP --&gt; Gov["Evaluations / Payments / Policy / Registry"]
  DP --&gt; RuntimeInvoke["Invoke Runtime / Harness"]
  DP --&gt; MemoryEvents["Events / Records / Extraction"]
  DP --&gt; ToolInvoke["Browser / Code Sessions"]
  DP --&gt; EvalRun["Evaluation / Payment Runtime APIs"]
  GP --&gt; ToolsList["MCP tools/list"]
  GP --&gt; ToolsCall["MCP tools/call"]
</whiteboard>

<h2>模块总览</h2>

<table>
  <thead>
    <tr><th background-color="light-gray">模块</th><th background-color="light-gray">资源边界</th><th background-color="light-gray">Control plane</th><th background-color="light-gray">Data plane 或特殊调用面</th></tr>
  </thead>
  <tbody>
    <tr><td>Runtime</td><td>agent runtime、runtime endpoint、runtime version、runtime session</td><td>创建 runtime、endpoint、版本与资源属性管理。</td><td>调用 runtime、runtime command、stop session、agent card。</td></tr>
    <tr><td>Harness</td><td>托管 agent loop 配置</td><td>创建和维护 harness 资源。</td><td><code>InvokeHarness</code> 执行托管 loop。</td></tr>
    <tr><td>Memory</td><td>memory、actor、session、event、record、extraction job</td><td>创建 memory 和策略配置。</td><td>写事件、查 actors/sessions、检索/批量维护 records、启动抽取任务。</td></tr>
    <tr><td>Gateway</td><td>gateway、target、rule、resource policy、policy engine</td><td>网关、目标、规则、资源策略、同步操作。</td><td>MCP gateway URL，语义为 <code>tools/list</code> 与 <code>tools/call</code>。</td></tr>
    <tr><td>Identity</td><td>workload identity、token vault、OAuth2/API key/payment credential provider</td><td>身份和凭据提供方 CRUD、Token Vault CMK。</td><td>换取 workload token、resource OAuth2 token、API key、payment token，完成授权回调。</td></tr>
    <tr><td>Browser</td><td>browser、browser profile、browser session</td><td>浏览器环境与 profile 管理。</td><td>启动/停止 session、invoke browser、更新 stream、保存 profile。</td></tr>
    <tr><td>Code Interpreter</td><td>code interpreter、session、tool invocation</td><td>代码解释器资源管理。</td><td>启动/停止 session，执行 code/command/file/task 类工具。</td></tr>
    <tr><td>Evaluations</td><td>evaluator、online config、batch evaluation、A/B test、recommendation</td><td>评测器、在线配置、配置包等资源。</td><td>在线 evaluate、batch job、A/B test、recommendation。</td></tr>
    <tr><td>Payments</td><td>payment manager、connector、credential provider、instrument、session</td><td>支付管理器、连接器、支付凭据提供方。</td><td>支付工具、支付会话、余额查询、支付处理。</td></tr>
    <tr><td>Policy / Registry</td><td>policy、policy engine、registry、registry record</td><td>策略、策略生成、注册目录、审批流。</td><td>registry records 搜索，运行时结合 Gateway/Policy engine 做治理。</td></tr>
  </tbody>
</table>

<h2>Runtime API</h2>

<p>Runtime 是把任意 agent 或 tool 部署到 AgentCore 托管运行环境的核心资源。OpenAPI 中应把控制面资源管理和数据面 invoke 分开建 tag。</p>

<table>
  <thead>
    <tr><th background-color="light-gray">OpenAPI / REST endpoint</th><th background-color="light-gray">SDK / CLI 语义</th><th background-color="light-gray">做什么</th><th background-color="light-gray">注意点</th></tr>
  </thead>
  <tbody>
    <tr><td><code>PUT /runtimes/</code></td><td><code>CreateAgentRuntime</code></td><td>创建 AgentCore Runtime。</td><td>关键字段包括 <code>agentRuntimeArtifact</code>、<code>agentRuntimeName</code>、<code>roleArn</code>、authorizer、network、lifecycle、protocol、filesystem、env vars。</td></tr>
    <tr><td><code>GET /runtimes/{agentRuntimeId}</code></td><td><code>GetAgentRuntime</code></td><td>读取 runtime 详情。</td><td>用于部署后轮询状态、读取 ARN、版本和 workload identity 详情。</td></tr>
    <tr><td><code>PUT /runtimes/{agentRuntimeId}</code></td><td><code>UpdateAgentRuntime</code></td><td>更新 runtime 配置。</td><td>版本、endpoint 与 session 行为需要和发布策略一起设计，避免更新影响活跃流量。</td></tr>
    <tr><td><code>DELETE /runtimes/{agentRuntimeId}</code></td><td><code>DeleteAgentRuntime</code></td><td>删除 runtime。</td><td>删除前应先检查 endpoint、session、观测与审计留存。</td></tr>
    <tr><td><code>GET /runtimes/</code></td><td><code>ListAgentRuntimes</code></td><td>列出 runtime。</td><td>建议支持 name、tag、status 维度的二次过滤。</td></tr>
    <tr><td><code>PUT /runtimes/{agentRuntimeId}/runtime-endpoints/</code></td><td><code>CreateAgentRuntimeEndpoint</code></td><td>创建 runtime endpoint。</td><td>endpoint 是版本与流量入口的关键抽象，可用于 default/prod/canary 等发布语义。</td></tr>
    <tr><td><code>GET/PUT/DELETE /runtimes/{agentRuntimeId}/runtime-endpoints/{endpointName}</code></td><td><code>Get/Update/DeleteAgentRuntimeEndpoint</code></td><td>读取、更新、删除 endpoint。</td><td>把 endpoint 生命周期和 runtime version 生命周期拆开管理。</td></tr>
    <tr><td><code>GET /runtimes/{agentRuntimeId}/runtime-versions</code></td><td><code>ListAgentRuntimeVersions</code></td><td>列出 runtime version。</td><td>用于回滚、审计和部署对比。</td></tr>
    <tr><td><code>POST /runtimes/{agentRuntimeArn}/invocations</code></td><td><code>InvokeAgentRuntime</code></td><td>向 runtime 内托管的 agent/tool 发送请求并实时接收响应。</td><td>支持 streaming/binary payload；Header 包含 runtime session、runtime user、MCP session、trace context 等，OAuth inbound 场景可能需要直接 HTTPS 调用。</td></tr>
    <tr><td><code>POST /runtimes/{agentRuntimeArn}/commands</code></td><td><code>InvokeAgentRuntimeCommand</code></td><td>向 runtime 发送命令。</td><td>适合作为运维/控制类入口，需严格区分用户请求和平台命令。</td></tr>
    <tr><td><code>POST /runtimes/{agentRuntimeArn}/stopruntimesession</code></td><td><code>StopRuntimeSession</code></td><td>停止 runtime session。</td><td>需要把 session id、用户 id、trace id 记录到审计链路。</td></tr>
    <tr><td><code>GET /.well-known/agent-card.json</code></td><td><code>GetAgentCard</code></td><td>读取 agent card。</td><td>用于 agent discovery / A2A 风格集成。</td></tr>
  </tbody>
</table>

<h2>Harness API</h2>

<p>Harness 是更上层的托管 agent loop：用户描述模型、prompt、tools、memory 等配置，平台负责 loop orchestration。它和 Runtime 的关系类似“托管 agent 编排配置”与“底层运行环境”。</p>

<table>
  <thead>
    <tr><th background-color="light-gray">OpenAPI / REST endpoint</th><th background-color="light-gray">SDK / CLI 语义</th><th background-color="light-gray">做什么</th><th background-color="light-gray">注意点</th></tr>
  </thead>
  <tbody>
    <tr><td><code>/harnesses</code></td><td><code>CreateHarness</code>、<code>ListHarnesses</code></td><td>创建或列出 harness。</td><td>OpenAPI 可先按资源集合表达；具体 path 以官方 API Reference 为准。</td></tr>
    <tr><td><code>/harnesses/{harnessIdentifier}</code></td><td><code>GetHarness</code>、<code>UpdateHarness</code>、<code>DeleteHarness</code></td><td>读取、更新、删除 harness。</td><td>建议把模型配置、工具配置、memory 绑定、版本信息拆成清晰 schema。</td></tr>
    <tr><td><code>Data plane: InvokeHarness</code></td><td><code>InvokeHarness</code></td><td>执行 harness 所定义的托管 agent loop。</td><td>调用结果与 trace/session/memory/tool 调用关系应在观测模型中串起来。</td></tr>
  </tbody>
</table>

<h2>Memory API</h2>

<p>Memory 同时覆盖短期事件存储和长期记忆抽取。建议在 OpenAPI 中拆成主资源、Events、Actors/Sessions、Memory Records、Extraction Jobs 五组。</p>

<h3>Memory 主资源</h3>

<table>
  <thead>
    <tr><th background-color="light-gray">OpenAPI / REST endpoint</th><th background-color="light-gray">SDK / CLI 语义</th><th background-color="light-gray">做什么</th><th background-color="light-gray">注意点</th></tr>
  </thead>
  <tbody>
    <tr><td><code>POST /memories/create</code></td><td><code>CreateMemory</code></td><td>创建 memory 资源。</td><td>关键字段包括 name、eventExpiryDuration、memoryStrategies、indexedKeys、execution role、KMS、stream delivery。</td></tr>
    <tr><td><code>GET/PUT/DELETE /memories/{memoryId}</code></td><td><code>GetMemory</code>、<code>UpdateMemory</code>、<code>DeleteMemory</code></td><td>读取、更新、删除 memory。</td><td>indexedKeys 一旦声明通常不宜轻易变更；策略变更要考虑已抽取记录兼容性。</td></tr>
    <tr><td><code>GET /memories/</code></td><td><code>ListMemories</code></td><td>列出 memory。</td><td>平台侧建议补充 owner/project/env/tag 过滤。</td></tr>
  </tbody>
</table>

<h3>Events / Actors / Sessions 子资源</h3>

<table>
  <thead>
    <tr><th background-color="light-gray">OpenAPI / REST endpoint</th><th background-color="light-gray">SDK / CLI 语义</th><th background-color="light-gray">做什么</th><th background-color="light-gray">注意点</th></tr>
  </thead>
  <tbody>
    <tr><td><code>POST /memories/{memoryId}/events</code></td><td><code>CreateEvent</code></td><td>写入用户、agent、tool 等 session event。</td><td>event schema 应保留 actor/session/branch、role、content、tool call、timestamp、metadata。</td></tr>
    <tr><td><code>GET /memories/{memoryId}/events/{eventId}</code></td><td><code>GetEvent</code></td><td>读取单个 event。</td><td>用于调试与审计，不应作为长期记忆检索主路径。</td></tr>
    <tr><td><code>GET /memories/{memoryId}/events</code></td><td><code>ListEvents</code></td><td>按 actor/session/时间列出 events。</td><td>注意分页、时间范围和 PII 可见性。</td></tr>
    <tr><td><code>DELETE /memories/{memoryId}/events/{eventId}</code></td><td><code>DeleteEvent</code></td><td>删除 event。</td><td>删除 event 与长期 record 的一致性需要明确策略。</td></tr>
    <tr><td><code>GET /memories/{memoryId}/actors</code></td><td><code>ListActors</code></td><td>列出 memory 下 actors。</td><td>actor 是用户、agent、系统主体的归属维度。</td></tr>
    <tr><td><code>GET /memories/{memoryId}/sessions</code></td><td><code>ListSessions</code></td><td>列出 sessions。</td><td>session 是短期上下文和长期记忆抽取的核心边界。</td></tr>
  </tbody>
</table>

<h3>Memory Records / Extraction Jobs 子资源</h3>

<table>
  <thead>
    <tr><th background-color="light-gray">OpenAPI / REST endpoint</th><th background-color="light-gray">SDK / CLI 语义</th><th background-color="light-gray">做什么</th><th background-color="light-gray">注意点</th></tr>
  </thead>
  <tbody>
    <tr><td><code>POST /memories/{memoryId}/retrieve</code></td><td><code>RetrieveMemoryRecords</code></td><td>按 query/filters 检索长期记忆。</td><td>这是 agent 推理前读记忆的主路径；需要记录检索命中、score、引用来源。</td></tr>
    <tr><td><code>GET /memories/{memoryId}/memoryRecords</code></td><td><code>ListMemoryRecords</code></td><td>列出长期记忆记录。</td><td>适合后台管理和调试，不替代 semantic retrieve。</td></tr>
    <tr><td><code>GET /memories/{memoryId}/memoryRecords/{recordId}</code></td><td><code>GetMemoryRecord</code></td><td>读取单条长期记忆。</td><td>应展示 version、source event、strategy、confidence、metadata。</td></tr>
    <tr><td><code>POST /memories/{memoryId}/memoryRecords/batch</code></td><td><code>BatchCreateMemoryRecords</code></td><td>批量创建记忆记录。</td><td>适合迁移、人工写入或离线抽取回灌。</td></tr>
    <tr><td><code>PATCH /memories/{memoryId}/memoryRecords/batch</code></td><td><code>BatchUpdateMemoryRecords</code></td><td>批量更新记忆记录。</td><td>更新必须保留审计和 version 关系。</td></tr>
    <tr><td><code>DELETE /memories/{memoryId}/memoryRecords/batch</code></td><td><code>BatchDeleteMemoryRecords</code></td><td>批量删除记忆记录。</td><td>删除长期记忆属于高风险操作，应有权限和二次确认。</td></tr>
    <tr><td><code>DELETE /memories/{memoryId}/memoryRecords/{recordId}</code></td><td><code>DeleteMemoryRecord</code></td><td>删除单条记忆记录。</td><td>需要明确是否保留 tombstone 和审计日志。</td></tr>
    <tr><td><code>POST /memories/{memoryId}/memoryExtractionJobs/start</code></td><td><code>StartMemoryExtractionJob</code></td><td>启动事件到长期记录的抽取任务。</td><td>memoryStrategies 可覆盖 semantic、episodic、summary、preference、custom 等策略。</td></tr>
    <tr><td><code>GET /memories/{memoryId}/memoryExtractionJobs</code></td><td><code>ListMemoryExtractionJobs</code></td><td>列出抽取任务。</td><td>用于追踪抽取状态、失败原因和重试。</td></tr>
  </tbody>
</table>

<h2>Gateway API</h2>

<p>Gateway 的控制面可用 OpenAPI 表达，数据面的工具调用应表达为 MCP 协议契约。把二者混在一个 REST path 表里会误导调用方。</p>

<h3>Gateway 主资源</h3>

<table>
  <thead>
    <tr><th background-color="light-gray">OpenAPI / REST endpoint</th><th background-color="light-gray">SDK / CLI 语义</th><th background-color="light-gray">做什么</th><th background-color="light-gray">注意点</th></tr>
  </thead>
  <tbody>
    <tr><td><code>POST /gateways/</code></td><td><code>CreateGateway</code></td><td>创建 gateway。</td><td>关键字段包括 name、protocolType、authorizerType、roleArn、policyEngineConfiguration、interceptorConfigurations、KMS、protocolConfiguration。</td></tr>
    <tr><td><code>GET /gateways/</code></td><td><code>ListGateways</code></td><td>列出 gateway。</td><td>建议按 project、env、protocol、auth type、owner 做治理视图。</td></tr>
    <tr><td><code>GET/PUT/DELETE /gateways/{gatewayIdentifier}</code></td><td><code>GetGateway</code>、<code>UpdateGateway</code>、<code>DeleteGateway</code></td><td>读取、更新、删除 gateway。</td><td>更新 authorizer、policy engine、interceptor 时要考虑对已有 target 与 client 的影响。</td></tr>
    <tr><td><code>PUT/GET/DELETE /gateways/{gatewayIdentifier}/resourcePolicy</code></td><td><code>PutResourcePolicy</code>、<code>GetResourcePolicy</code>、<code>DeleteResourcePolicy</code></td><td>维护 gateway/resource 级访问策略。</td><td>资源策略与 IAM policy、gateway authorizer 是叠加关系。</td></tr>
    <tr><td><code>POST /gateways/{gatewayIdentifier}/rules</code></td><td><code>CreateGatewayRule</code></td><td>创建 gateway rule。</td><td>用于控制或拦截 gateway 调用流程。</td></tr>
    <tr><td><code>GET/PUT/DELETE /gateways/{gatewayIdentifier}/rules/{ruleId}</code></td><td><code>GetGatewayRule</code>、<code>UpdateGatewayRule</code>、<code>DeleteGatewayRule</code></td><td>读取、更新、删除 rule。</td><td>规则变更应有审批与灰度。</td></tr>
  </tbody>
</table>

<h3>Gateway Targets 子资源</h3>

<table>
  <thead>
    <tr><th background-color="light-gray">OpenAPI / REST endpoint</th><th background-color="light-gray">SDK / CLI 语义</th><th background-color="light-gray">做什么</th><th background-color="light-gray">注意点</th></tr>
  </thead>
  <tbody>
    <tr><td><code>POST /gateways/{gatewayIdentifier}/targets/</code></td><td><code>CreateGatewayTarget</code></td><td>把 Lambda、OpenAPI、Smithy、MCP 等目标接入 gateway。</td><td>target schema 决定暴露出的 MCP tools，必须关注 operationId、tool name、auth、参数 schema。</td></tr>
    <tr><td><code>GET /gateways/{gatewayIdentifier}/targets/</code></td><td><code>ListGatewayTargets</code></td><td>列出 targets。</td><td>建议展示 source type、sync status、tool count、last sync。</td></tr>
    <tr><td><code>GET/PUT/DELETE /gateways/{gatewayIdentifier}/targets/{targetId}</code></td><td><code>GetGatewayTarget</code>、<code>UpdateGatewayTarget</code>、<code>DeleteGatewayTarget</code></td><td>读取、更新、删除 target。</td><td>target 变更可能影响工具名和参数契约，需纳入版本兼容检查。</td></tr>
    <tr><td><code>PUT /gateways/{gatewayIdentifier}/synchronizeTargets</code></td><td><code>SynchronizeGatewayTargets</code></td><td>同步 gateway targets。</td><td>OpenAPI/Smithy 源变化后需要同步，平台应记录差异和失败原因。</td></tr>
  </tbody>
</table>

<h3>Gateway MCP 调用面</h3>

<table>
  <thead>
    <tr><th background-color="light-gray">OpenAPI / REST endpoint</th><th background-color="light-gray">SDK / CLI 语义</th><th background-color="light-gray">做什么</th><th background-color="light-gray">注意点</th></tr>
  </thead>
  <tbody>
    <tr><td><code>https://{gatewayId}.gateway.bedrock-agentcore.{region}.amazonaws.com</code></td><td>MCP endpoint</td><td>Gateway 数据面入口。</td><td>不要把它展开成伪 REST path；按 MCP client 初始化和会话语义处理。</td></tr>
    <tr><td><code>MCP tools/list</code></td><td>工具发现</td><td>列出 target 同步出的工具清单。</td><td>工具名、description、inputSchema、auth requirement 来自 target 和 gateway 配置。</td></tr>
    <tr><td><code>MCP tools/call</code></td><td>工具调用</td><td>调用某个 target 暴露出来的工具。</td><td>IAM 中有 <code>InvokeGateway</code> 权限动作；outbound auth 由 Identity credential provider 或 target 配置承接。</td></tr>
  </tbody>
</table>

<h2>Identity API</h2>

<p>Identity 是 AgentCore 把“平台工作负载身份”和“代表用户访问外部资源的凭据”分离管理的核心。OpenAPI 设计上建议把 identity provider、credential provider、token vault、data plane token exchange 分成不同 tag。</p>

<table>
  <thead>
    <tr><th background-color="light-gray">OpenAPI / REST endpoint</th><th background-color="light-gray">SDK / CLI 语义</th><th background-color="light-gray">做什么</th><th background-color="light-gray">注意点</th></tr>
  </thead>
  <tbody>
    <tr><td><code>/workload-identities</code></td><td><code>CreateWorkloadIdentity</code>、<code>ListWorkloadIdentities</code></td><td>创建或列出 workload identity。</td><td>runtime、gateway、tool 调用的主体身份应与最终用户身份分开。</td></tr>
    <tr><td><code>/workload-identities/{identityId}</code></td><td><code>GetWorkloadIdentity</code>、<code>UpdateWorkloadIdentity</code>、<code>DeleteWorkloadIdentity</code></td><td>维护 workload identity。</td><td>身份更新影响调用授权，需审计。</td></tr>
    <tr><td><code>/oauth2-credential-providers</code></td><td><code>Create/Get/List/Update/DeleteOauth2CredentialProvider</code></td><td>维护 OAuth2 凭据提供方。</td><td>支持 2LO/3LO 类 outbound auth 时，token refresh 与 consent 应作为平台能力。</td></tr>
    <tr><td><code>/api-key-credential-providers</code></td><td><code>Create/Get/List/Update/DeleteApiKeyCredentialProvider</code></td><td>维护 API key 凭据提供方。</td><td>不要把 API key 写进 prompt、tool schema 或 runtime env；通过 provider 引用。</td></tr>
    <tr><td><code>/payment-credential-providers</code></td><td><code>Create/Get/List/Update/DeletePaymentCredentialProvider</code></td><td>维护 payment credential provider。</td><td>支付相关凭据需要单独合规边界。</td></tr>
    <tr><td><code>/token-vault/cmk</code></td><td><code>SetTokenVaultCMK</code>、<code>GetTokenVault</code></td><td>设置或读取 Token Vault 加密配置。</td><td>KMS key 是凭据治理的硬前置。</td></tr>
    <tr><td><code>Data plane token APIs</code></td><td><code>GetWorkloadAccessToken</code>、<code>GetWorkloadAccessTokenForJWT</code>、<code>GetWorkloadAccessTokenForUserId</code></td><td>获取 workload access token。</td><td>区分服务身份、JWT 映射身份、用户代理身份。</td></tr>
    <tr><td><code>Data plane resource credential APIs</code></td><td><code>GetResourceOauth2Token</code>、<code>GetResourceApiKey</code>、<code>GetResourcePaymentToken</code>、<code>CompleteResourceTokenAuth</code></td><td>获取外部资源访问凭据或完成授权。</td><td>只向受控 tool/runtime 暴露短期凭据，禁止直接回显给模型。</td></tr>
  </tbody>
</table>

<h2>Browser API</h2>

<p>Browser 提供隔离托管浏览器环境。控制面负责创建 browser/profile，数据面负责 session 与交互。</p>

<table>
  <thead>
    <tr><th background-color="light-gray">OpenAPI / REST endpoint</th><th background-color="light-gray">SDK / CLI 语义</th><th background-color="light-gray">做什么</th><th background-color="light-gray">注意点</th></tr>
  </thead>
  <tbody>
    <tr><td><code>/browsers</code></td><td><code>CreateBrowser</code>、<code>ListBrowsers</code></td><td>创建或列出 browser 环境。</td><td>控制面资源用于定义隔离浏览器能力边界。</td></tr>
    <tr><td><code>/browsers/{browserId}</code></td><td><code>GetBrowser</code>、<code>DeleteBrowser</code></td><td>读取或删除 browser。</td><td>删除前检查活跃 session。</td></tr>
    <tr><td><code>/browser-profiles</code></td><td><code>CreateBrowserProfile</code>、<code>ListBrowserProfiles</code></td><td>创建或列出 browser profile。</td><td>profile 可持久化登录态/偏好，需强权限和数据隔离。</td></tr>
    <tr><td><code>/browser-profiles/{profileId}</code></td><td><code>GetBrowserProfile</code>、<code>DeleteBrowserProfile</code></td><td>读取或删除 profile。</td><td>profile 属于敏感状态资产。</td></tr>
    <tr><td><code>PUT /browsers/{browserIdentifier}/sessions/start</code></td><td><code>StartBrowserSession</code></td><td>启动 browser session。</td><td>可配置 viewport、proxy、extension、certificate、enterprise policy、profile。</td></tr>
    <tr><td><code>GET /browsers/{browserIdentifier}/sessions/{sessionId}</code></td><td><code>GetBrowserSession</code></td><td>读取 session 状态。</td><td>用于轮询和调试。</td></tr>
    <tr><td><code>GET /browsers/{browserIdentifier}/sessions</code></td><td><code>ListBrowserSessions</code></td><td>列出 browser sessions。</td><td>需要限制用户只能看有权访问的 session。</td></tr>
    <tr><td><code>POST /browsers/{browserIdentifier}/tools/invoke</code></td><td><code>InvokeBrowser</code></td><td>执行浏览器动作。</td><td>返回截图、DOM/文本、动作结果等；与 trace/span 关联。</td></tr>
    <tr><td><code>POST /browsers/{browserIdentifier}/stream</code></td><td><code>UpdateBrowserStream</code></td><td>更新 browser stream。</td><td>用于 live viewer / streaming 体验。</td></tr>
    <tr><td><code>POST /browsers/{browserIdentifier}/sessions/{sessionId}/save-profile</code></td><td><code>SaveBrowserSessionProfile</code></td><td>保存 session 状态到 profile。</td><td>保存登录态是敏感操作，需审批或用户授权。</td></tr>
    <tr><td><code>POST /browsers/{browserIdentifier}/sessions/{sessionId}/stop</code></td><td><code>StopBrowserSession</code></td><td>停止 browser session。</td><td>清理临时文件、cookies、trace 和截图引用。</td></tr>
  </tbody>
</table>

<h2>Code Interpreter API</h2>

<p>Code Interpreter 提供隔离代码执行环境。数据面围绕 session 和工具调用展开，工具名由 <code>InvokeCodeInterpreter</code> 的 <code>name</code> 字段指定。</p>

<table>
  <thead>
    <tr><th background-color="light-gray">OpenAPI / REST endpoint</th><th background-color="light-gray">SDK / CLI 语义</th><th background-color="light-gray">做什么</th><th background-color="light-gray">注意点</th></tr>
  </thead>
  <tbody>
    <tr><td><code>/code-interpreters</code></td><td><code>CreateCodeInterpreter</code>、<code>ListCodeInterpreters</code></td><td>创建或列出 code interpreter。</td><td>控制面负责定义执行环境与边界。</td></tr>
    <tr><td><code>/code-interpreters/{codeInterpreterId}</code></td><td><code>GetCodeInterpreter</code>、<code>DeleteCodeInterpreter</code></td><td>读取或删除 code interpreter。</td><td>删除前检查活跃 session 和文件留存策略。</td></tr>
    <tr><td><code>PUT /code-interpreters/{codeInterpreterIdentifier}/sessions/start</code></td><td><code>StartCodeInterpreterSession</code></td><td>启动代码执行 session。</td><td>session id 后续通过 header <code>x-amzn-code-interpreter-session-id</code> 传递。</td></tr>
    <tr><td><code>GET /code-interpreters/{codeInterpreterIdentifier}/sessions/{sessionId}</code></td><td><code>GetCodeInterpreterSession</code></td><td>读取 session 状态。</td><td>用于异步任务和运行态诊断。</td></tr>
    <tr><td><code>GET /code-interpreters/{codeInterpreterIdentifier}/sessions</code></td><td><code>ListCodeInterpreterSessions</code></td><td>列出 sessions。</td><td>需按用户、runtime、project 做隔离。</td></tr>
    <tr><td><code>POST /code-interpreters/{codeInterpreterIdentifier}/tools/invoke</code></td><td><code>InvokeCodeInterpreter</code></td><td>执行代码、命令、文件和任务工具。</td><td>工具名包括 <code>executeCode</code>、<code>executeCommand</code>、<code>readFiles</code>、<code>listFiles</code>、<code>removeFiles</code>、<code>writeFiles</code>、<code>startCommandExecution</code>、<code>getTask</code>、<code>stopTask</code>。</td></tr>
    <tr><td><code>POST /code-interpreters/{codeInterpreterIdentifier}/sessions/{sessionId}/stop</code></td><td><code>StopCodeInterpreterSession</code></td><td>停止代码执行 session。</td><td>停止时要处理临时文件、任务、stdout/stderr、artifact 留存。</td></tr>
  </tbody>
</table>

<h2>Evaluations API</h2>

<p>Evaluations 覆盖在线评测、批量评测、A/B test 与推荐优化。它不是单个 API，而是一组质量闭环资源。</p>

<table>
  <thead>
    <tr><th background-color="light-gray">OpenAPI / REST endpoint</th><th background-color="light-gray">SDK / CLI 语义</th><th background-color="light-gray">做什么</th><th background-color="light-gray">注意点</th></tr>
  </thead>
  <tbody>
    <tr><td><code>/evaluators</code></td><td><code>Create/Get/List/Update/DeleteEvaluator</code></td><td>维护 evaluator。</td><td>evaluator 是评测逻辑、指标、模型/规则的封装。</td></tr>
    <tr><td><code>/online-evaluation-configs</code></td><td><code>Create/Get/List/Update/DeleteOnlineEvaluationConfig</code></td><td>维护在线评测配置。</td><td>适合生产流量采样和持续质量观测。</td></tr>
    <tr><td><code>/configuration-bundles</code></td><td><code>Create/Get/List/Update/DeleteConfigurationBundle</code>、<code>Get/ListConfigurationBundleVersion(s)</code></td><td>维护评测配置包和版本。</td><td>用于多环境复用与回滚。</td></tr>
    <tr><td><code>Data plane: Evaluate</code></td><td><code>Evaluate</code></td><td>对单次输入/输出或会话片段执行评测。</td><td>建议输出 score、reason、evidence、metric id。</td></tr>
    <tr><td><code>Data plane: BatchEvaluation</code></td><td><code>Start/Get/List/Stop/DeleteBatchEvaluation</code></td><td>离线批量评测任务。</td><td>适合 golden tasks、回归测试、版本发布门禁。</td></tr>
    <tr><td><code>Data plane: ABTest</code></td><td><code>Create/Get/List/Update/DeleteABTest</code></td><td>A/B test 资源。</td><td>可用于 prompt、model、tool config、runtime version 对比。</td></tr>
    <tr><td><code>Data plane: Recommendation</code></td><td><code>Start/Get/List/DeleteRecommendation</code></td><td>根据评测结果生成优化建议。</td><td>建议与变更建议、实验、审批流程打通。</td></tr>
  </tbody>
</table>

<h2>Payments API</h2>

<p>Payments 把 agent 访问付费内容、付费 API 或支付流程纳入平台能力。该模块合规风险高，OpenAPI 落地应单独审查。</p>

<table>
  <thead>
    <tr><th background-color="light-gray">OpenAPI / REST endpoint</th><th background-color="light-gray">SDK / CLI 语义</th><th background-color="light-gray">做什么</th><th background-color="light-gray">注意点</th></tr>
  </thead>
  <tbody>
    <tr><td><code>/payment-managers</code></td><td><code>Create/Get/List/Update/DeletePaymentManager</code></td><td>维护 payment manager。</td><td>作为支付域配置入口。</td></tr>
    <tr><td><code>/payment-connectors</code></td><td><code>Create/Get/List/Update/DeletePaymentConnector</code></td><td>维护 payment connector。</td><td>连接外部支付网络/供应商。</td></tr>
    <tr><td><code>/payment-credential-providers</code></td><td><code>Create/Get/List/Update/DeletePaymentCredentialProvider</code></td><td>维护支付凭据提供方。</td><td>需与 Token Vault、KMS、审计策略联动。</td></tr>
    <tr><td><code>Data plane: payment instruments</code></td><td><code>Create/Get/List/DeletePaymentInstrument</code>、<code>GetPaymentInstrumentBalance</code></td><td>维护支付工具并查询余额。</td><td>支付工具是敏感资产，不应直接暴露给 agent prompt。</td></tr>
    <tr><td><code>Data plane: payment sessions</code></td><td><code>Create/Get/List/DeletePaymentSession</code></td><td>创建和管理支付会话。</td><td>需要用户授权、风控和会话状态机。</td></tr>
    <tr><td><code>Data plane: ProcessPayment</code></td><td><code>ProcessPayment</code></td><td>发起支付处理。</td><td>生产环境需要金额、收款方、用途、审批、幂等、回滚和对账。</td></tr>
  </tbody>
</table>

<h2>Policy / Registry API</h2>

<p>Policy 与 Registry 负责把 agent、tools、策略、资产目录纳入组织治理。它们更接近平台治理平面，不应只当作附属字段。</p>

<table>
  <thead>
    <tr><th background-color="light-gray">OpenAPI / REST endpoint</th><th background-color="light-gray">SDK / CLI 语义</th><th background-color="light-gray">做什么</th><th background-color="light-gray">注意点</th></tr>
  </thead>
  <tbody>
    <tr><td><code>/policies</code></td><td><code>Create/Get/List/Update/DeletePolicy</code>、<code>Get/ListPolicySummary</code></td><td>维护策略资源及摘要。</td><td>策略应覆盖 gateway/tool/runtime/session/user 等维度。</td></tr>
    <tr><td><code>/policy-engines</code></td><td><code>Create/Get/List/Update/DeletePolicyEngine</code>、<code>Get/ListPolicyEngineSummary</code></td><td>维护策略引擎。</td><td>Gateway 可绑定 policy engine configuration。</td></tr>
    <tr><td><code>/policy-generations</code></td><td><code>StartPolicyGeneration</code>、<code>Get/ListPolicyGeneration(s)</code>、<code>Get/ListPolicyGenerationSummary</code>、<code>ListPolicyGenerationAssets</code></td><td>策略生成与资产追踪。</td><td>可用于从自然语言或资产分析生成策略草稿，但仍需审批。</td></tr>
    <tr><td><code>/registries</code></td><td><code>Create/Get/List/Update/DeleteRegistry</code></td><td>维护 registry。</td><td>注册目录可承载 tools、agents、templates、policies 等组织资产。</td></tr>
    <tr><td><code>/registries/{registryId}/records</code></td><td><code>Create/Get/List/Update/DeleteRegistryRecord</code>、<code>UpdateRegistryRecordStatus</code>、<code>SubmitRegistryRecordForApproval</code></td><td>维护 registry records 和审批状态。</td><td>适合 tool/agent 上架、审核、禁用、版本治理。</td></tr>
    <tr><td><code>Data plane: SearchRegistryRecords</code></td><td><code>SearchRegistryRecords</code></td><td>搜索 registry records。</td><td>agent/tool discovery 可从 registry search 开始，但执行仍需权限校验。</td></tr>
  </tbody>
</table>

<h2>上手调用顺序速查</h2>

<table>
  <thead>
    <tr><th background-color="light-gray">场景</th><th background-color="light-gray">推荐调用顺序</th><th background-color="light-gray">落地检查点</th></tr>
  </thead>
  <tbody>
    <tr><td>部署并调用 Runtime</td><td><code>CreateWorkloadIdentity</code> → <code>CreateAgentRuntime</code> → <code>CreateAgentRuntimeEndpoint</code> → <code>InvokeAgentRuntime</code> → <code>StopRuntimeSession</code></td><td>roleArn、authorizer、network、runtime version、endpoint、trace/session header、超时和停止语义。</td></tr>
    <tr><td>接入长期记忆</td><td><code>CreateMemory</code> → <code>CreateEvent</code> → <code>StartMemoryExtractionJob</code> → <code>RetrieveMemoryRecords</code> → <code>BatchUpdate/DeleteMemoryRecords</code></td><td>actor/session 归属、event schema、strategy、record provenance、删除与合规。</td></tr>
    <tr><td>把企业 API 暴露成工具</td><td><code>CreateOauth2/APIKeyCredentialProvider</code> → <code>CreateGateway</code> → <code>CreateGatewayTarget</code> → <code>SynchronizeGatewayTargets</code> → MCP <code>tools/list</code> / <code>tools/call</code></td><td>OpenAPI operationId、target sync diff、gateway authorizer、outbound auth、InvokeGateway 权限。</td></tr>
    <tr><td>浏览器自动化</td><td><code>CreateBrowser</code> → <code>CreateBrowserProfile</code> → <code>StartBrowserSession</code> → <code>InvokeBrowser</code> → <code>SaveBrowserSessionProfile</code> → <code>StopBrowserSession</code></td><td>profile 敏感性、代理/证书/扩展、live stream、截图与 DOM 证据留存。</td></tr>
    <tr><td>代码沙箱执行</td><td><code>CreateCodeInterpreter</code> → <code>StartCodeInterpreterSession</code> → <code>InvokeCodeInterpreter</code> → <code>GetCodeInterpreterSession</code> → <code>StopCodeInterpreterSession</code></td><td>session 文件边界、工具名白名单、异步 task、输出 artifact、资源限制。</td></tr>
    <tr><td>质量闭环</td><td><code>CreateEvaluator</code> → <code>CreateOnlineEvaluationConfig</code> / <code>StartBatchEvaluation</code> → <code>Evaluate</code> → <code>CreateABTest</code> → <code>StartRecommendation</code></td><td>golden set、指标 schema、版本对比、上线门禁、建议回写流程。</td></tr>
    <tr><td>组织治理</td><td><code>CreatePolicyEngine</code> → <code>CreatePolicy</code> / <code>StartPolicyGeneration</code> → <code>CreateRegistry</code> → <code>CreateRegistryRecord</code> → <code>SubmitRegistryRecordForApproval</code></td><td>审批、策略生效范围、registry asset lifecycle、搜索和执行权限分离。</td></tr>
  </tbody>
</table>

<whiteboard type="mermaid">sequenceDiagram
  participant Dev as Developer
  participant CP as AgentCore Control Plane
  participant Id as Identity
  participant Rt as Runtime
  participant Mem as Memory
  participant Gw as Gateway
  participant Obs as CloudWatch / OTEL
  Dev-&gt;&gt;Id: CreateWorkloadIdentity / credential provider
  Dev-&gt;&gt;CP: CreateAgentRuntime and endpoint
  Dev-&gt;&gt;Mem: CreateMemory with strategies
  Dev-&gt;&gt;Gw: CreateGateway and targets
  Dev-&gt;&gt;Rt: InvokeAgentRuntime
  Rt-&gt;&gt;Mem: CreateEvent / RetrieveMemoryRecords
  Rt-&gt;&gt;Gw: MCP tools/list and tools/call
  Rt-&gt;&gt;Obs: traces / logs / metrics
  Rt--&gt;&gt;Dev: streaming or final response
</whiteboard>

<h2>官方来源入口</h2>

<ul>
  <li><a href="https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/what-is-bedrock-agentcore.html">Overview - Amazon Bedrock AgentCore</a></li>
  <li><a href="https://docs.aws.amazon.com/bedrock-agentcore/latest/APIReference/API_Operations.html">Actions - Amazon Bedrock AgentCore Data Plane</a></li>
  <li><a href="https://docs.aws.amazon.com/bedrock-agentcore-control/latest/APIReference/API_Operations.html">Actions - Amazon Bedrock AgentCore Control Plane</a></li>
  <li><a href="https://docs.aws.amazon.com/service-authorization/latest/reference/list_amazonbedrockagentcore.html">Actions, resources, and condition keys for Amazon Bedrock AgentCore</a></li>
  <li><a href="https://docs.aws.amazon.com/general/latest/gr/bedrock_agentcore.html">Amazon Bedrock AgentCore endpoints and quotas</a></li>
  <li><a href="https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/gateway-schema-openapi.html">Gateway OpenAPI schema targets</a></li>
  <li><a href="https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/gateway-building-smithy-targets.html">Gateway Smithy model targets</a></li>
  <li><a href="https://raw.githubusercontent.com/aws/aws-sdk-go-v2/main/service/bedrockagentcore/generated.json">AWS SDK Go v2 bedrockagentcore generated.json</a></li>
  <li><a href="https://raw.githubusercontent.com/aws/aws-sdk-go-v2/main/service/bedrockagentcorecontrol/generated.json">AWS SDK Go v2 bedrockagentcorecontrol generated.json</a></li>
</ul>
